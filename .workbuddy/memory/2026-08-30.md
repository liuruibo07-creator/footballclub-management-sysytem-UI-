# 2026-08-30

## FC-UI 登录页与导航栏微调
- `src/layout/components/Navbar.vue:84`：退出登录确认弹窗文案由「确定注销并退出系统吗？」改为「确定退出系统吗？」
- `src/views/login.vue`：登录页输入框（账号/密码/验证码）背景由 `#f7faff` 改为纯白 `#ffffff`，加 `!important` 覆盖全局暗色主题
- `src/views/login.vue`：输入框与验证码按钮去掉实体 border，改用 `box-shadow: 0 0 0 1px` 实现描边（hover 浅蓝、focus 主蓝+光晕），所有 box-shadow 也加 `!important`
- `src/views/login.vue`：`el-input__inner` / placeholder / `el-select__placeholder` 等深色文字规则统一加 `!important` 改回深色（`#1f2c3d` / `#94a3b8`），input-icon 颜色 `#6b7a8c`

## 关键发现：暗色主题样式覆盖
- `src/assets/styles/dark-console.scss` 用 `!important` 全局覆盖 Element Plus 组件样式（包括 `.el-input__wrapper` 的 `background: #0c1725`、`.el-input__inner` 的浅色文字色等）
- 任何想脱离暗色主题的页面，**必须给相关 Element Plus 样式也加 `!important`**，否则修改不会生效
- 主要覆盖范围：`.el-input__wrapper` / `.el-textarea__inner` / `.el-select__wrapper` / 表格 / 按钮 / 分页 / 表单 label / 滚动条 等

## 登录页大改造（保留部分）
执行了改进建议 8-13 中除 #8、#9 之外的所有改动：
- **完整队徽 SVG**（80×80，虎头 + 圆形外框 + 蓝白渐变），虎眼 4.2s 眨眼睛动效
- **登录按钮动效**：内嵌足球 SVG 图标，hover 时 360° 旋转；点击 ripple 涟漪（白圈扩散 600ms）
- **表单 a11y**：每个 input 加 `id` + `aria-label` + 视觉隐藏的 `<label for>`，加 `.sr-only` 工具类
- **进场动效**：`.login-form` 用 `cardIn` keyframes（opacity + translateY + scale）
- **自动聚焦**：`onMounted` + `usernameRef.focus()` 让页面进入即聚焦账号框
- **安全规范链接**：底部"俱乐部数据安全规范"改为可点击 `.security-link` 蓝色虚线下划线链接

## 部分还原：左侧 hero 区
用户最后选择**回滚左侧 hero 标语 + 数据卡片**（改进 8 + 9）：
- 移除 template 中 `<section class="hero">` 整块
- 删除 `.hero` / `.hero-content` / `.hero-title` / `.hero-line` / `.hero-since` / `.hero-stats` / `.stat-card` / `.stat-num` / `.stat-label` / `.stat-divider` / `.hero-foot` 全部样式
- 删除 `@keyframes heroIn`
- `.login` 布局从 `justify-content: space-between; gap: 40px` 还原为 `justify-content: flex-end`
- `.login::after` 渐变从 0.55/0.38/0.65（用于压暗左侧文本）还原为 0.02/0.05/0.32/0.58（原来的"卡片区压暗"）
- 清理 `@media (max-width: 900px)` 中的 `.hero { display: none; }`

保留：`.login-form` 上的 `cardIn` 入场动画、队徽 SVG 改造、按钮动效、a11y、自动聚焦、ripple、安全链接。
